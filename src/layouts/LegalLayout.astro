---
import BaseLayout from '@layouts/BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
}
const { title, description } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <section class="legal-page">
    <div class="container">
      <a href="/" class="legal-back">← Zurück zur Startseite</a>
      <slot />
    </div>
  </section>
</BaseLayout>

<style is:global>
  .legal-page { padding: 120px 0 80px; }
  .legal-page .container { max-width: 760px; }
  .legal-page h1 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 2rem;
    color: var(--text-bright);
  }
  .legal-page h2 {
    font-size: 1.15rem;
    font-weight: 600;
    margin-top: 2rem;
    margin-bottom: 0.5rem;
    color: var(--text-bright);
  }
  .legal-page h3 {
    font-size: 1rem;
    font-weight: 600;
    margin-top: 1.5rem;
    margin-bottom: 0.5rem;
    color: var(--text-bright);
  }
  .legal-page p {
    margin-bottom: 0.75rem;
    color: var(--text);
  }
  .legal-page ul {
    margin: 0 0 0.75rem 1.25rem;
    padding: 0;
    color: var(--text);
  }
  .legal-page li {
    margin-bottom: 0.35rem;
    color: var(--text);
  }
  .legal-page a {
    color: var(--red);
    /* permanent underline — links must be distinguishable by more than color (WCAG 1.4.1; axe link-in-text-block: red-on-gray was 2.85:1) */
    text-decoration: underline;
    text-underline-offset: 2px;
    text-decoration-color: rgba(229, 57, 53, 0.55);
  }
  .legal-page a:hover { text-decoration-color: var(--red); }
  .legal-stand {
    font-size: 0.9rem;
    color: var(--text-dim);
    margin-bottom: 1.5rem;
  }
  .legal-back {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: var(--text-dim);
    text-decoration: none;
    font-size: 0.9rem;
    margin-bottom: 1.5rem;
    transition: color 0.2s;
  }
  .legal-back:hover { color: var(--red); }
  .info-box {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 1.5rem;
    margin-bottom: 2rem;
  }
  .info-box p { margin-bottom: 0.25rem; }
  .info-box strong { color: var(--text-bright); }
</style>
