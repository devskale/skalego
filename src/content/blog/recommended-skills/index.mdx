---
title: Recommended Skills
description: One-line installs for the agent skills we recommend — curl skale.dev/s/<name> | bash
date: 2025-07-15
author: johann
tags: [skills, agents, pi]
draft: false
skills:
  - slug: fetch-url
    desc: "Readable Text von jeder Webseite holen: Artikel, Docs, Reddit, GitHub, StackOverflow."
    install: pi-skill
    details:
      how: "Wählt je nach Seite das beste Backend: zuerst lokale, freie Tools (w3m, lynx), dann freie APIs (jina, markdown), dann Headless-Chrome für Cloudflare- und JS-geschützte Seiten. Installiert ein fetch-url-CLI in ~/.local/bin, das sich alle 7 Tage selbst aktualisiert."
  - slug: web-search
    desc: "Websuche über SearXNG und Duck API. Bilder, News, Videos. Funktioniert ohne Konfiguration."
    install: pi-skill
    details:
      how: "Wählt das Backend automatisch: SearXNG (frei, ohne Credentials) oder die Duck API (erweiterte Filter: --site, --filetype, --inurl). Unterstützt Bilder, News, Videos, Zeitraum, Region und JSON-Ausgabe. Optionaler privater SearXNG via `credgoo searx`."
  - slug: surf
    desc: "Dein echtes, eingeloggtes Chrome auf macOS steuern: scrapen, klicken, ausfüllen, Screenshots. Ohne Daemon, ohne Debug-Port."
    install: pi-skill
    details:
      how: "macOS AppleScript plus Chromes „Allow JavaScript from Apple Events“. Kein Daemon, kein Debug-Port, keine Extension, kein Verbindungsdialog. Cookies, Sessions und Tabs bleiben intakt. Ziel ist der aktive Tab; `surf select w1.t3` pinnt einen Hintergrund-Tab."
  - slug: impeccable
    desc: "Design-Skill: Frontend-UI formen, kritisieren, härten und polieren, mit Anti-Pattern-Detektor. Cross-Harness."
    install: "command:npx impeccable install"
    details:
      how: "Liest ein Design-System-JSON (Tokens, Komponenten, Anti-Patterns) und steuert die Kommandos critique, harden und live. Von @pbakaus. Installiert über `npx impeccable install` nach .agents/."
  - slug: improve-architecture
    desc: "Architektur-Review-Familie: Deepening-Chancen scannen, HTML-Report, Grill-Durchgang. Installiert 7 Skills (inkl. batch-grill-me) nach .agents/skills/. Von @mattpocock."
    install: "command:npx -y skills@latest add https://github.com/mattpocock/skills --skill improve-codebase-architecture codebase-design domain-modeling grilling grill-me grill-with-docs batch-grill-me -a codex -y </dev/null"
    details:
      steps:
        - "Explore: die letzte Git-History nach Hot Spots abgehen; per Subagent flache Module finden (Interface so komplex wie die Implementierung), fehlende Lokalität, undichte Nähte. Deletion-Test anwenden."
        - "Present candidates: eigenständigen HTML-Report (Tailwind + Mermaid) ins Temp-Verzeichnis schreiben, eine Karte pro Kandidat: Dateien, Problem, Lösung, Nutzen, Vorher/Nachher-Diagramm, Strength-Badge. Report öffnen, dann fragen, welcher Kandidat vertieft wird."
        - "Grill: den /grilling-Skill auf den gewählten Kandidaten anwenden (Entscheidungsbaum: Constraints, Abhängigkeiten, Naht-Form, überlebende Tests). /domain-modeling inline laufen lassen, damit CONTEXT.md und ADRs aktuell bleiben."
  - slug: implement
    desc: "Arbeit aus Spec oder Tickets umsetzen: TDD an den Nähten, Typecheck unterwegs, /code-review am Ende. Installiert implement + tdd + code-review. Von @mattpocock."
    install: "command:npx -y skills@latest add https://github.com/mattpocock/skills --skill implement tdd code-review -a codex -y </dev/null"
    details:
      how: "TDD an vorab vereinbarten Nähten, wo möglich. Typprüfung regelmäßig, einzelne Testdateien häufig, die ganze Suite einmal am Ende. Schließt mit /code-review und committet auf den aktuellen Branch."
  - slug: web
    desc: "Das Web-Recherche-Bundle: fetch-url, web-search und surf in einem Install."
    install: "pi-skillset:fetch-url,web-search,surf"
    details:
      how: "Installiert alle drei Skills auf einmal über die pi-Paket-Whitelist. Zusammen decken sie die Recherche-Kette ab: Seite lesen (fetch-url), Seiten finden (web-search), eigenen Browser steuern (surf)."
  - slug: gakefay
    desc: "Test-Skill: macht nichts. Dient dem End-to-End-Test des Install-Flows."
    install: "command:echo '✓ gakefay installed (no-op test skill)'"
    hidden: true
---

We ship a small set of agent skills for [pi](https://pi.farm) and recommend a few
from upstream. Each installs with one line:

```bash
curl -fsSL https://skale.dev/s/<name> | bash
```

Our own skills (`fetch-url`, `web-search`, `surf`) enable via pi's package
whitelist; external ones (`impeccable`, …) run their upstream installer. Browse
all installable skills on [**/skills**](/skills/).

### `improve-architecture` — a 7-skill family

One install pulls in [@mattpocock](https://github.com/mattpocock)'s architecture-review family — seven skills that work together:

- **`improve-codebase-architecture`** — scan for deepening opportunities, render an HTML report, then grill through the one you pick.
- **`codebase-design`** — the shared deep-module vocabulary (module, interface, depth, seam, adapter) the others speak.
- **`domain-modeling`** — maintain the project's domain model (`CONTEXT.md` terms, ADRs).
- **`grilling`** — the relentless decision-tree interview at the core of the grill step.
- **`grill-me`** — a relentless interview to sharpen a plan or design.
- **`grill-with-docs`** — grilling + domain-modeling combined, writing ADRs/glossary as it goes.
- **`batch-grill-me`** — a relentless interview that asks every frontier question at once, round by round.

Install all seven:

```bash
curl -fsSL https://skale.dev/s/improve-architecture | bash
```

They land in `.agents/skills/` (pi reads it automatically).

### `implement` — spec/ticket → committed code

Another [@mattpocock](https://github.com/mattpocock) skill: implement a piece of work from a spec or tickets. TDD at pre-agreed seams where possible, typecheck regularly, run the full suite once at the end, then `/code-review` and commit to the current branch.

```bash
curl -fsSL https://skale.dev/s/implement | bash
```

Once installed, manage **skills and extensions** any time with `pi config` (interactive TUI — space toggles, Tab switches scope) or `pi config -l` (opens straight in project scope). No re-install needed to turn things on or off.
