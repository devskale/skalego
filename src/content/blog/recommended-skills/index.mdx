---
title: Recommended Skills
description: One-line installs for the agent skills we recommend — curl skale.dev/s/<name> | bash
date: 2025-07-15
author: johann
tags: [skills, agents, pi]
draft: false
skills:
  - slug: fetch-url
    desc: "Fetch and extract readable text from any web page — articles, docs, Reddit, GitHub, StackOverflow."
    install: pi-skill
    details:
      what: "Extracts readable text from any web page — articles, docs, Reddit, GitHub, StackOverflow."
      how: "Auto-selects the best backend per site: free local tools first (w3m, lynx), then free APIs (jina, markdown), then headless Chrome for Cloudflare/JS-protected pages. Ships a `fetch-url` CLI in ~/.local/bin; auto-updates every 7 days."
  - slug: web-search
    desc: "Web search via SearXNG + Duck API. Images, news, videos. Works out of the box."
    install: pi-skill
    details:
      what: "Web search with automatic backend selection — works out of the box with public SearXNG."
      how: "Picks SearXNG (free, no creds) or the Duck API (advanced filters: --site, --filetype, --inurl). Supports images/news/videos, time-range, region, JSON output. Optional private SearXNG via `credgoo searx`."
  - slug: surf
    desc: "Drive your real, logged-in Google Chrome on macOS — scrape, click, fill, screenshot. No daemon, no debug port."
    install: pi-skill
    details:
      what: "Drive your real, logged-in Google Chrome from the CLI on macOS — scrape, click, fill, screenshot."
      how: "macOS AppleScript + Chrome's 'Allow JavaScript from Apple Events'. No daemon, no debug port, no extension, no per-connection dialog. Cookies/sessions/tabs stay intact. Targets the active tab; `surf select w1.t3` pins a background tab."
  - slug: web
    desc: "The web research + browser bundle: fetch-url + web-search + surf. Installs all three."
    install: "pi-skillset:fetch-url,web-search,surf"
    details:
      what: "The web research + browser bundle: fetch-url + web-search + surf in one install."
      how: "Installs all three skills at once via pi's package whitelist. Together they cover fetch (read a page), search (find pages), and surf (drive your logged-in browser)."
  - slug: impeccable
    desc: "Design skill — shape, critique, harden, and polish frontend UI + anti-pattern detector. Cross-harness."
    install: "command:npx impeccable install"
    details:
      what: "Design skill — shape, critique, harden, and polish frontend UI + anti-pattern detector. Cross-harness."
      how: "Reads a design-system JSON (tokens, components, anti-patterns) and runs critique/harden/live commands. By @pbakaus. Installs via `npx impeccable install` into .agents/."
  - slug: improve-architecture
    desc: "Architecture review family — scan for deepening opportunities, HTML report, grill-through. Installs 7 skills (the family incl. batch-grill-me) into .agents/skills/. By @mattpocock."
    install: "command:npx -y skills@latest add https://github.com/mattpocock/skills --skill improve-codebase-architecture codebase-design domain-modeling grilling grill-me grill-with-docs batch-grill-me -a codex -y </dev/null"
    details:
      what: "Architecture review family — scan for deepening opportunities, HTML report, grill-through. 7 skills by @mattpocock."
      steps:
        - "Explore — walk recent git history for hot spots; use a subagent to find shallow modules (interface as complex as implementation), missing locality, leaky seams. Apply the deletion test."
        - "Present candidates — write a self-contained HTML report (Tailwind + Mermaid) to the OS temp dir, one card per candidate: files, problem, solution, benefits, before/after diagram, strength badge. Open it, then ask which to explore."
        - "Grill — run the /grilling skill on the picked candidate (decision tree: constraints, dependencies, seam shape, surviving tests). Run /domain-modeling inline to keep CONTEXT.md and ADRs current as decisions crystallize."
  - slug: implement
    desc: "Implement work from a spec or tickets — TDD at seams, typecheck as you go, /code-review at the end. Installs implement + tdd + code-review. By @mattpocock."
    install: "command:npx -y skills@latest add https://github.com/mattpocock/skills --skill implement tdd code-review -a codex -y </dev/null"
    details:
      what: "Implement a piece of work based on a spec or set of tickets. Installs the full set: implement + tdd + code-review."
      how: "TDD at pre-agreed seams where possible. Runs typechecking regularly, single test files regularly, and the full suite once at the end. Finishes with /code-review, then commits to the current branch."
  - slug: gakefay
    desc: "Test skill — does nothing. Used to verify the install flow end-to-end."
    install: "command:echo '✓ gakefay installed (no-op test skill)'"
    hidden: true
---

We ship a small set of agent skills for [pi](https://pi.farm) and recommend a few
from upstream. Each installs with one line:

```bash
curl -fsSL https://skale.dev/s/<name> | bash
```

Our own skills (`fetch-url`, `web-search`, `surf`) enable via pi's package
whitelist; external ones (`impeccable`, …) run their upstream installer. Browse
all installable skills on [**/skills**](/skills/).

### `improve-architecture` — a 7-skill family

One install pulls in [@mattpocock](https://github.com/mattpocock)'s architecture-review family — seven skills that work together:

- **`improve-codebase-architecture`** — scan for deepening opportunities, render an HTML report, then grill through the one you pick.
- **`codebase-design`** — the shared deep-module vocabulary (module, interface, depth, seam, adapter) the others speak.
- **`domain-modeling`** — maintain the project's domain model (`CONTEXT.md` terms, ADRs).
- **`grilling`** — the relentless decision-tree interview at the core of the grill step.
- **`grill-me`** — a relentless interview to sharpen a plan or design.
- **`grill-with-docs`** — grilling + domain-modeling combined, writing ADRs/glossary as it goes.
- **`batch-grill-me`** — a relentless interview that asks every frontier question at once, round by round.

Install all seven:

```bash
curl -fsSL https://skale.dev/s/improve-architecture | bash
```

They land in `.agents/skills/` (pi reads it automatically).

### `implement` — spec/ticket → committed code

Another [@mattpocock](https://github.com/mattpocock) skill: implement a piece of work from a spec or tickets. TDD at pre-agreed seams where possible, typecheck regularly, run the full suite once at the end, then `/code-review` and commit to the current branch.

```bash
curl -fsSL https://skale.dev/s/implement | bash
```

Once installed, manage **skills and extensions** any time with `pi config` (interactive TUI — space toggles, Tab switches scope) or `pi config -l` (opens straight in project scope). No re-install needed to turn things on or off.
